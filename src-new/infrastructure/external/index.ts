/**
 * External / Migrated Code
 *
 * This folder will contain code migrated from the old `src/` directory
 * that doesn't fit cleanly into services or adapters.
 *
 * For Phase 3:
 * - Copy src/token-manager.ts → external/token-manager.ts (if using)
 * - Copy src/github-utils.ts → external/github-utils.ts (if using)
 *
 * These are kept as reference implementations and gradually refactored
 * into proper service implementations.
 *
 * In the new architecture:
 * - token-manager logic is in TokenServiceImpl
 * - github-utils logic is in GitHubServiceImpl
 * - crypto utilities are in CryptoServiceImpl
 *
 * Eventually, this folder should be empty.
 */

export {};
