/**
 * Refactor Placeholder (Phase 7: session/new handler)
 * --------------------------------------------------
 * Creates a new session (delegates to SessionStore and maybe WorkspaceService).
 */

export async function sessionNewHandler(_params: any): Promise<any> {
  // eslint-disable-line @typescript-eslint/no-explicit-any
  throw new Error(
    'sessionNewHandler not implemented (refactor phase 7 placeholder)',
  );
}

export default sessionNewHandler;
