# LumiLink Integration - Worker authenticates using your JWT token
# Backend securely manages GitHub App credentials at platform level
LUMILINK_API_URL=http://localhost:8787
LUMILINK_JWT_TOKEN=your_lumilink_jwt_token_here

# Encryption key for sensitive user data (Anthropic API keys, etc.)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your_32_byte_hex_encryption_key_here

# Optional default Anthropic API key for fallback operations
ANTHROPIC_API_KEY=sk-ant-api03-your_anthropic_key_here

# OpenRouter API key for ACP bridge operations
OPENROUTER_API_KEY=sk-or-v1-your_openrouter_key_here

# LangSmith Cost Tracking (optional)
# Enable tracing to track LLM costs and performance
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=ls_...
LANGSMITH_PROJECT=lumilink-code-mode

# Container provider selection ("cloudflare" or "daytona")
CONTAINER_PROVIDER=cloudflare
# Daytona API credentials (required when CONTAINER_PROVIDER=daytona)
DAYTONA_API_URL=https://your-daytona-instance/api
DAYTONA_API_KEY=your_daytona_api_key (admin_key)
